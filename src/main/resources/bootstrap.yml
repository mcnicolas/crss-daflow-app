spring:
  application:
    name: crss-dataflow-app
  cloud:
    config:
      discovery:
        enabled: false
        service-id: config-server
      fail-fast: false

---
spring:
  profiles: qa
  cloud:
    config:
      uri: http://config-server.marathon.l4lb.thisdcos.directory:8080
      label: crss2
